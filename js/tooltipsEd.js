require(["dijit/Tooltip", "dojo/domReady!"], function(Tooltip) {
	// create a new Tooltip and connect it
	new Tooltip({
		connectId : ["hartfordDiv"],
		label : "this is the tooltip of hartford"
	});
	// create a new Tooltip and connect it
	new Tooltip({
		connectId : ["newhavenDiv"],
		label : "this is the tooltip of newhaven"
	});
	// create a new Tooltip and connect it
	new Tooltip({
		connectId : ["shorelineDiv"],
		label : "this is the tooltip of shoreline"
	});
	// create a new Tooltip and connect it
	new Tooltip({
		connectId : ["allTranSysDiv"],
		label : "this is the tooltip of allTranSys"
	});
	// create a new Tooltip and connect it
	new Tooltip({
		connectId : ["loadfDiv"],
		label : "this is the tooltip of loadf"
	});
	// create a new Tooltip and connect it
	new Tooltip({
		connectId : ["vehicleDiv"],
		label : "this is the tooltip of vehicle"
	});
	// create a new Tooltip and connect it
	new Tooltip({
		connectId : ["stopAmeDiv"],
		label : "this is the tooltip of stopAme"
	});
	// create a new Tooltip and connect it
	new Tooltip({
		connectId : ["onTimeDiv"],
		label : "this is the tooltip of onTime"
	});
	// create a new Tooltip and connect it
	new Tooltip({
		connectId : ["headAwayDiv"],
		label : "this is the tooltip of headAway"
	});
	// create a new Tooltip and connect it
	new Tooltip({
		connectId : ["allPerformDiv"],
		label : "this is the tooltip of allPerform"
	});
	// create a new Tooltip and connect it
	new Tooltip({
		connectId : ["minorityDiv"],
		label : "this is the tooltip of minority"
	});
	// create a new Tooltip and connect it
	new Tooltip({
		connectId : ["nonMinoDiv"],
		label : "this is the tooltip of nonMino"
	});
	// create a new Tooltip and connect it
	new Tooltip({
		connectId : ["lowIncomeDiv"],
		label : "this is the tooltip of lowIncome"
	});
	// create a new Tooltip and connect it
	new Tooltip({
		connectId : ["nonLowInDiv"],
		label : "this is the tooltip of nonLowIn"
	});
	// create a new Tooltip and connect it
	new Tooltip({
		connectId : ["LEPDiv"],
		label : "this is the tooltip of LEP"
	});
	// create a new Tooltip and connect it
	new Tooltip({
		connectId : ["allDemoGrDiv"],
		label : "this is the tooltip of allDemoGr"
	});
	// create a new Tooltip and connect it
	new Tooltip({
		connectId : ["blockGrDiv"],
		label : "this is the tooltip of blockGr"
	});// create a new Tooltip and connect it
	new Tooltip({
		connectId : ["censusTrDiv"],
		label : "this is the tooltip of censusTr"
	});
	// create a new Tooltip and connect it
	new Tooltip({
		connectId : ["selectLocalDiv"],
		label : "this is the tooltip of selec local bus"
	});
	new Tooltip({
		connectId : ["selectExpressDiv"],
		label : "this is the tooltip of selec express bus"
	});
	new Tooltip({
		connectId : ["selectRailDiv"],
		label : "this is the tooltip of selec rail service"
	});
}); 